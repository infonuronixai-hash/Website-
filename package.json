{
  "name": "vaibhava-tech-website",
  "version": "1.0.0",
  "description": "Vaibhava Tech company website (static site + Cloudflare Pages Functions)",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && wrangler pages dev dist"
  },
  "dependencies": {
    "hbs": "^4.2.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
